name: "91 - Merge PullRequest"

on: pull_request_target

permissions:
  id-token: write
  pull-requests: write
  contents: write

jobs:
  run-tests:
    name: Test-and-Build
    uses: ./.github/workflows/21-test-chatapp.yml
    secrets: inherit

